# Design System / Shared UI Theme

_Placeholder — the final design system is not defined yet._

The shared frontend lives under [`shared/frontend/`](../../shared/frontend/):

- `index.html` — shared landing page (placeholder).
- `css/main.css` — minimal shared theme, present only to verify the shared
  CSS/theme pipeline works.
- `js/` — shared JavaScript (empty).
- `assets/` — shared static assets (empty).

## To be agreed

- Colour palette, typography, and spacing scale (currently placeholder tokens
  in `css/main.css`).
- Component patterns (nav, cards, forms, tables) built with HTML5 + HTMX.
- Accessibility baseline.
- How each student's feature frontend consumes the shared theme.

Do not build out the full visual design system until the team has agreed the
direction.
