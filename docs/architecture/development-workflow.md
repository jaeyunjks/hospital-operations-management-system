# Development Workflow

_Placeholder — to be agreed by the team._

Suggested starting conventions (subject to team agreement):

## Ownership boundaries

- Each student works primarily within their own `student-N/` directory.
- Changes to `shared/` or `ai-services/` are coordinated with the team.
- Each student maintains their own `.github/workflows/student-N.yml`.

## Branching & reviews

- _TBD_ — branching model (e.g. feature branches, PRs into `main`).
- _TBD_ — review requirements before merge.

## Local environment

- Python 3.x, Docker & Docker Compose, Git, VS Code.
- Ollama installed locally with an approved model pulled for AI-Mode work.

## Definition of done (per feature, later)

- CRUD implemented and tested.
- Dockerfile builds.
- Individual CI/CD workflow passes.
- Feature integrated via `docker-compose.yml` and the shared frontend.

Fill this in as the team establishes its actual process.
