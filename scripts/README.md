# Scripts

Helper scripts for the project. **Placeholders only.**

| Directory  | Purpose                                             |
|------------|-----------------------------------------------------|
| `build/`   | Build helpers (images / assets)                     |
| `test/`    | Test helpers (run per-service or integrated tests)  |
| `deploy/`  | Deployment helpers (Azure Container Apps, later)    |

No scripts implemented yet — add them as the build/test/deploy processes are
defined.
