# Shared Configuration (placeholder)

Team-wide configuration shared across services (e.g. shared environment
templates, common settings). **Empty scaffold.**

Never commit secrets, API keys, or local environment files here — see the
project `.gitignore`. Provide non-secret templates (e.g. `*.example`) only.
