# ==========================================================================
# student-1 microservice — Dockerfile (PLACEHOLDER)
# ==========================================================================
# This is a placeholder. student-1 has no application code yet, so no build
# instructions are active — this file will NOT build until the service exists.
#
# When implementing, use a Flask microservice image along these lines:
#
#   FROM python:3.12-slim
#   WORKDIR /app
#   COPY backend/requirements.txt .
#   RUN pip install --no-cache-dir -r requirements.txt
#   COPY backend/ .
#   EXPOSE 5000
#   CMD ["python", "app.py"]
#
# Keep this Dockerfile scoped to student-1 only.
# ==========================================================================
