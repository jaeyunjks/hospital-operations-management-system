"""Prompt artefacts for the Student 5 backend/API microservice.

Prompts live here rather than inline in route or service code so that each one
is a reviewable artefact in its own right: versioned, diffable, and citable
from the prompt engineering record in ``docs/prompts/student-5/``.
"""
